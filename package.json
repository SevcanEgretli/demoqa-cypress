{
  "name": "demoqa-cypress",
  "version": "1.0.0",
  "description": "Cypress E2E tests for DemoQA practice form",
  "main": "index.js",
  "scripts": {
    "test": "cypress run",
    "test:headed": "cypress run --headed --browser chrome",
    "report": "npx mochawesome-merge cypress/reports/*.json > merged-report.json && npx marge merged-report.json --reportDir cypress/reports/html"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^22.0.1",
    "@bahmutov/cypress-esbuild-preprocessor": "^2.2.5",
    "@cypress/webpack-preprocessor": "^6.0.4",
    "cypress": "^14.3.3",
    "esbuild": "^0.25.4",
    "mochawesome": "^7.1.3",
    "mochawesome-merge": "^5.0.0",
    "mochawesome-report-generator": "^6.2.0"
  }
}
